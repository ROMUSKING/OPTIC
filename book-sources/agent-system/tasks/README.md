# Task Packet Flow

This directory is the repository’s explicit task graph.

Recommended subdirectories:
- `inbox/` — not yet decomposed
- `active/` — currently owned and being worked
- `review/` — waiting for coherence audit or human approval
- `done/` — completed and archived packets

The template lives at `../TASK_PACKET_TEMPLATE.md`.
