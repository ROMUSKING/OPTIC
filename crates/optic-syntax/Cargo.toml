[package]
name = "optic-syntax"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
thiserror.workspace = true
serde.workspace = true

[dev-dependencies]
# For golden tests later
# insta = "1"

[features]
default = []
# future: full recovery, etc.