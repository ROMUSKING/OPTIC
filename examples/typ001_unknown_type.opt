-- TYP-001: unknown costate type not declared in program

data Entities {
    healths: SoA<f32>,
}

optic GhostView: GradedOptic<GhostWorld, f32, _> {
    get  s      => s.healths[s.id]
    put  (s, v) => { s.healths[s.id] = v }
}

fn main() {
    entities.query(GhostView).get();
}