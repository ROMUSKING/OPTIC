-- TYP-001: unknown focus type not declared in program

data Entities {
    healths: SoA<f32>,
}

optic BadFocus: GradedOptic<Entities, GhostScalar, _> {
    get  s      => s.healths[s.id]
    put  (s, v) => { s.healths[s.id] = v }
}

fn main() {
    entities.query(BadFocus).get();
}